import { DiffDateToTodayPipe } from './diff-date-to-today.pipe';

describe('DiffDateToTodayPipe', () => {
  it('create an instance', () => {
    const pipe = new DiffDateToTodayPipe();
    expect(pipe).toBeTruthy();
  });
});
