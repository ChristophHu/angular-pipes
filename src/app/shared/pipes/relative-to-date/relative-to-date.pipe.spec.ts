import { RelativeToDatePipe } from './relative-to-date.pipe';

describe('RelativeToDatePipe', () => {
  it('create an instance', () => {
    const pipe = new RelativeToDatePipe();
    expect(pipe).toBeTruthy();
  });
});
