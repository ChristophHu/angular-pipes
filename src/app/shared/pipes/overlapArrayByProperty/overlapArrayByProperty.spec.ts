import { SliceArrayByPropertyPipe } from './overlapArrayByProperty';

describe('SliceArrayByPropertyPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceArrayByPropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
